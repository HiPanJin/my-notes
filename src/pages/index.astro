---
import { getCollection } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import Socials from "@/components/Socials.astro";
import LinkButton from "@/components/LinkButton.astro";
import Card from "@/components/Card.astro";
import getSortedPosts from "@/utils/getSortedPosts";
import IconRss from "@/assets/icons/IconRss.svg";
import IconArrowRight from "@/assets/icons/IconArrowRight.svg";
import { SITE } from "@/config";
import { SOCIALS } from "@/constants";

const posts = await getCollection("blog");

const sortedPosts = getSortedPosts(posts);
const featuredPosts = sortedPosts.filter(({ data }) => data.featured);
const recentPosts = sortedPosts.filter(({ data }) => !data.featured);
---

<Layout>
  <Header />
  <main
    id="main-content"
    data-layout="index"
    class="mx-auto w-full max-w-app px-3 sm:px-4"
  >
    <div
      class="paper-sheet my-5 rounded-[2px] border border-border/80 px-5 py-7 sm:my-8 sm:px-10 sm:py-10"
    >
      <section id="hero" class="px-0 pt-0 pb-7">
        <div class="mb-5 flex items-start justify-between gap-4">
          <h1
            class="manuscript-title my-0 max-w-2xl text-[2.65rem] leading-[1.08] text-accent sm:text-6xl"
          >
            Jin Pan's notes
          </h1>
          <a
            target="_blank"
            href="/rss.xml"
            class="mt-2 inline-block rounded-[2px] p-1 text-accent transition hover:-rotate-3 hover:text-foreground"
            aria-label="rss feed"
            title="RSS Feed"
          >
            <IconRss
              width={20}
              height={20}
              class="scale-125 stroke-current stroke-3 rtl:-rotate-90"
            />
            <span class="sr-only">RSS Feed</span>
          </a>
        </div>

        <p class="max-w-2xl text-lg leading-9 text-foreground sm:text-xl">
          One topic, one living page. I keep short notes in place instead of
          turning every thought into a new article.
        </p>
        <p class="margin-note mt-4 max-w-xl">
          Mostly tiny realizations, rough edges, and sentences worth keeping
          before they slip away.
        </p>
        {
          // only display if at least one social link is enabled
          SOCIALS.length > 0 && (
            <div class="note-rule mt-7 flex flex-col pt-4 sm:flex-row sm:items-center">
              <div class="margin-note me-2 mb-1 whitespace-nowrap sm:mb-0">
                Margin notes:
              </div>
              <Socials />
            </div>
          )
        }
      </section>

      {
        featuredPosts.length > 0 && (
          <>
            <div class="note-rule my-8" />
            <section id="featured" class="px-0 pt-0 pb-4">
              <h2 class="manuscript-title text-3xl leading-tight text-accent sm:text-4xl">
                置顶笔记
              </h2>
              <ul>
                {featuredPosts.map(data => (
                  <Card variant="h3" basePath="/notes" {...data} />
                ))}
              </ul>
            </section>
          </>
        )
      }

      {
        recentPosts.length > 0 && (
          <>
            <div class="note-rule my-8" />
            <section id="recent-posts" class="px-0 pt-0 pb-4">
              <h2 class="manuscript-title text-3xl leading-tight text-accent sm:text-4xl">
                最近笔记
              </h2>
              <ul>
                {recentPosts.map(
                  (data, index) =>
                    index < SITE.postPerIndex && (
                      <Card variant="h3" basePath="/notes" {...data} />
                    )
                )}
              </ul>
            </section>
          </>
        )
      }

      <div class="mt-7 text-center">
        <LinkButton
          href="/notes/"
          class="ink-link inline-flex items-center gap-2 border-b border-dashed border-accent/70 pb-1"
        >
          Open all notes
          <IconArrowRight class="inline-block rtl:-rotate-180" />
        </LinkButton>
      </div>
    </div>
  </main>
  <Footer />
</Layout>

<script>
  document.addEventListener("astro:page-load", () => {
    const indexLayout = (document.querySelector("#main-content") as HTMLElement)
      ?.dataset?.layout;
    if (indexLayout) {
      sessionStorage.setItem("backUrl", "/");
    }
  });
</script>
